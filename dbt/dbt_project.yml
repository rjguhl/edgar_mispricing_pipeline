name: "edgar_mispricing"
version: "1.0.0"
config-version: 2

profile: "edgar_mispricing"

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  edgar_mispricing:
    staging:
      +materialized: view
      +schema: staging
    features:
      +materialized: table
      +schema: features
